# Listen on Railway's $PORT, fall back to 80 locally
:{$PORT:80} {
    root * /srv
    encode gzip
    log {
        output stdout
    }

    # SPA fallback for client-side routing
    try_files {path} /index.html
    file_server
}
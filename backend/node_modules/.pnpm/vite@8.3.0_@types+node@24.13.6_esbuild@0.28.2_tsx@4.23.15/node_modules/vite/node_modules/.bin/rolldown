#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../rolldown/bin/cli.mjs" "$@"
else
  exec node  "$basedir/../../../rolldown/bin/cli.mjs" "$@"
fi
# cmd-shim-target=/Users/rakesh/project_lab/ai/odin_project/backend/node_modules/.pnpm/vite@8.3.0_@types+node@24.13.6_esbuild@0.28.2_tsx@4.23.15/node_modules/rolldown/bin/cli.mjs

#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../oxlint-tsgolint/bin/tsgolint.js" "$@"
else
  exec node  "$basedir/../../../oxlint-tsgolint/bin/tsgolint.js" "$@"
fi
# cmd-shim-target=/Users/rakesh/project_lab/ai/odin_project/backend/node_modules/.pnpm/oxlint@1.85.0_oxlint-tsgolint@7.0.2002/node_modules/oxlint-tsgolint/bin/tsgolint.js

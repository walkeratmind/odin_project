#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../prettier/bin/prettier.cjs" "$@"
else
  exec node  "$basedir/../../../../prettier/bin/prettier.cjs" "$@"
fi
# cmd-shim-target=/Users/rakesh/project_lab/ai/odin_project/backend/node_modules/.pnpm/@nestjs+schematics@12.0.4_chokidar@5.0.0_prettier@3.9.8_typescript@6.0.3/node_modules/prettier/bin/prettier.cjs
